import { Canvas, Story, ArgsTable } from '@storybook/addon-docs';

# Checkbox 

This component is a customization of the `radix/checkbox`, it renders just that. 

Obs: `Label` must be inserted separately.

### Default

<Canvas withToolbar>
  <Story id="components-checkbox--default" />
</Canvas>

<ArgsTable story="^" />